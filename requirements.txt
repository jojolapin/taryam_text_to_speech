# TextSpeak Pro - runtime dependencies
# Python 3.10 - 3.13 recommended (cp39-abi3 wheels are forward-compatible)

# Desktop shell (PySide6 6.7+ bundles QtWebEngine Chromium)
PySide6>=6.7.0

# Offline neural TTS engine (ships Windows wheels since 1.3.0)
piper-tts>=1.4.2

# Audio encoders
lameenc>=1.7.0          # MP3 (libmp3lame)

# Audio metadata (ID3 tags on exported MP3s)
mutagen>=1.47.0

# Input imports
pypdf>=4.0.0            # PDF text extraction

# HTTP downloads (voice catalog)
requests>=2.31.0
