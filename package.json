{
  "name": "textspeak-pro-tests",
  "version": "1.1.0",
  "private": true,
  "description": "Dev-only JS regression tests for TextSpeak Pro. Uses the Node built-in test runner (no dependencies). Not shipped in the app.",
  "scripts": {
    "test": "node --test \"tests/js/**/*.test.js\""
  }
}
